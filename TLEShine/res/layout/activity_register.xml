<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.tranway.tleshine.viewLoginAndRegister.RegisterActivity" >

    <include
        android:id="@+id/include_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        layout="@layout/title_control" />

    <ScrollView
        android:id="@+id/register_form"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white" >

        <LinearLayout
            style="@style/RegisterFormContainer"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/txt_tips"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:gravity="center"
                android:text="@string/register_accout_tips"
                android:textColor="@color/black"
                android:textSize="@dimen/text_size_middle" />

            <EditText
                android:id="@+id/email"
                style="@style/edittext_userinfo"
                android:layout_marginTop="50dp"
                android:background="@drawable/border_square"
                android:hint="@string/prompt_email"
                android:imeOptions="actionNext"
                android:inputType="textEmailAddress"
                android:textCursorDrawable="@null" />

            <EditText
                android:id="@+id/password"
                style="@style/edittext_userinfo"
                android:layout_marginTop="20dp"
                android:background="@drawable/border_square"
                android:hint="@string/prompt_password"
                android:imeOptions="actionNext"
                android:inputType="textPassword"
                android:textCursorDrawable="@null" />

            <EditText
                android:id="@+id/confirm_password"
                style="@style/edittext_userinfo"
                android:layout_marginTop="20dp"
                android:background="@drawable/border_square"
                android:hint="@string/prompt_confirm_password"
                android:imeOptions="actionDone"
                android:inputType="textPassword"
                android:textCursorDrawable="@null" />
        </LinearLayout>
    </ScrollView>

</LinearLayout>